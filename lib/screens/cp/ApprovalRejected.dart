import 'package:flutter/material.dart';

class ApprovalRejected extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child : Text('rejected')
    );
  }
}
